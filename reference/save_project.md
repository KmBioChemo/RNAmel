# Save a project to disk

Save a project to disk

## Usage

``` r
save_project(project, path)
```

## Arguments

- project:

  a project list (from
  [`empty_project()`](https://KmBioChemo.github.io/RNAmel/reference/empty_project.md)
  or session)

- path:

  file path (will get .rnaflow.rds extension if missing)

## Value

invisibly returns the path written to
